version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - unconvert
    - bodyclose
    - nilerr

formatters:
  enable:
    - gofmt
    - goimports

run:
  timeout: 5m
